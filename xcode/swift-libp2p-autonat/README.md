# swift-libp2p-autonat

AutoNAT v1 signal source and client probe helper for libp2p.

The package exposes `/libp2p/autonat/1.0.0`, updates a local reachability
signal, and can probe AutoNAT-capable peers to determine whether the node
appears publicly reachable.
