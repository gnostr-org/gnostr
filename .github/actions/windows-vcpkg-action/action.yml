## - name: Setup and Test
##   uses: ./.github/actions/windows-vcpkg-action
##   with:
##     arch: 'x64'
##     run-tests: 'true'

name: 'windows-vcpkg-action'
description: 'Setup vcpkg, OpenSSL, and Perl with maximum verbosity'
inputs:
  arch:
    description: 'Target architecture (e.g., x64)'
    required: true
  run-tests:
    description: 'Whether to run cargo test'
    required: false
    default: 'false'

runs:
  using: 'composite'
  steps:
    - name: Set Environment and Verbosity
      shell: pwsh
      run: |
        # Use the standard vcpkg installation root provided by the runner
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        echo "OPENSSL_NO_VENDOR=1" | Out-File -FilePath $env:GITHUB_ENV -Append

        # This ensures inner build processes can see these vars
        echo "VCPKG_KEEP_ENV_VARS=OPENSSL_NO_VENDOR;VCPKG_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append

        # VCVARSALL_DEBUG helps debug MSVC compiler detection
        echo "VCVARSALL_DEBUG=1" | Out-File -FilePath $env:GITHUB_ENV -Append

    - name: Install OpenSSL with vcpkg (Verbose)
      shell: pwsh
      run: |
        # Using --verbose and --debug instead of --printbuildtree
        vcpkg install openssl:${{ inputs.arch }}-windows-static-md --verbose --debug

    - name: Install Strawberry Perl
      shell: pwsh
      run: |
        choco install strawberryperl -y --verbose
        echo "C:\Strawberry\perl\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        echo "C:\Strawberry\c\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      if: runner.os == 'Windows'

    - name: Install Perl modules
      shell: pwsh
      run: perl -MCPAN -e "install Locale::Maketext::Simple"
      if: runner.os == 'Windows'

    - name: Run Cargo Test (Very Verbose)
      shell: pwsh
      run: |
        # Ensure we are looking for the vcpkg installation
        $env:VCPKGRS_DYNAMIC = "1"
        # -vv displays the actual output of build.rs scripts (e.g., from openssl-sys)
        cargo test -vv --all-features
      if: ${{ inputs.run-tests == 'true' }}
